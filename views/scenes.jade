div.card
  div.card-content    
    span.card-title.black-text My Scenes
    div.row
      div.col.m12.s12
        ul.collection
          each s in scenes
            a.modal-trigger(href='#scenePreview', onClick='loadPreview()')
              li.collection-item= s.title
        //- - for (var i = 0; i < 100; i++ ) {
        //-     a.modal-trigger(href='#scenePreview')
        //-       img.thumbnail(src='/images/caffeine.jpg')
        //- - }
